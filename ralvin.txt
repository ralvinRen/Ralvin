<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Ren Jiaxuan - macOS Portfolio</title>
    
    <!-- React & Tailwind -->
    <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Fonts & Icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { sans: ['Inter', '-apple-system', 'sans-serif'] },
                    colors: {
                        'mac-glass': 'rgba(255, 255, 255, 0.75)',
                        'mac-glass-dark': 'rgba(30, 30, 30, 0.65)',
                        'mac-blue': '#007AFF',
                        'mac-gray': '#F5F5F5',
                    },
                    boxShadow: {
                        'mac-window': '0 20px 50px rgba(0,0,0,0.3)',
                        'mac-dock': '0 10px 20px rgba(0,0,0,0.15)',
                    },
                    animation: {
                        'scale-up': 'scaleUp 0.3s cubic-bezier(0.16, 1, 0.3, 1)',
                        'fade-in': 'fadeIn 0.5s ease-out',
                    },
                    keyframes: {
                        scaleUp: { '0%': { transform: 'scale(0.95)', opacity: 0 }, '100%': { transform: 'scale(1)', opacity: 1 } },
                        fadeIn: { '0%': { opacity: 0 }, '100%': { opacity: 1 } }
                    }
                }
            }
        }
    </script>
    <style>
        body { margin: 0; overflow: hidden; background-color: #000; user-select: none; }
        
        .wallpaper {
            background: url('https://images.unsplash.com/photo-1621574539436-4b0d00bb34c2?q=80&w=2560&auto=format&fit=crop') no-repeat center center fixed;
            background-size: cover;
        }
        .lockscreen-bg {
            background-image: url('starting page.gif'); 
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
        }
        .glass { backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); background: rgba(255, 255, 255, 0.85); border: 1px solid rgba(255, 255, 255, 0.4); }
        .glass-dark { backdrop-filter: blur(20px); background: rgba(30, 30, 30, 0.6); border-bottom: 1px solid rgba(255, 255, 255, 0.1); color: white; }
        .no-scrollbar::-webkit-scrollbar { display: none; }
        .finder-item-selected { background-color: #007AFF; color: white; }
        .finder-item:hover:not(.finder-item-selected) { background-color: #E5E5E5; }
        .desktop-icon { width: 90px; padding: 10px; border-radius: 8px; transition: background 0.2s; display: flex; flex-direction: column; align-items: center; cursor: default; position: relative; }
        .desktop-icon:hover { background-color: rgba(255,255,255,0.2); }
        .desktop-icon:active { background-color: rgba(255,255,255,0.3); }
        .desktop-icon-img { filter: drop-shadow(0 4px 8px rgba(0,0,0,0.2)); pointer-events: none; }
        
        /* Admin Editing Styles */
        .edit-input { border: 1px solid #007AFF; border-radius: 4px; padding: 4px 8px; width: 100%; outline: none; font-size: 13px; background: white; margin-bottom: 8px; }
        .edit-textarea { border: 1px solid #007AFF; border-radius: 4px; padding: 8px; width: 100%; min-height: 80px; outline: none; font-size: 13px; resize: none; background: white; margin-bottom: 8px; }
        .edit-label { display: block; font-size: 10px; font-weight: bold; color: #888; text-transform: uppercase; margin-bottom: 2px; }
        
        .resize-handle { position: absolute; bottom: 0; right: 0; width: 20px; height: 20px; cursor: nwse-resize; z-index: 100; }
        .resize-handle::after { content: ''; position: absolute; bottom: 4px; right: 4px; width: 8px; height: 8px; border-bottom: 2px solid #CCC; border-right: 2px solid #CCC; border-radius: 0 0 2px 0; }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect, useRef } = React;

        // --- 1. 初始数据常量 ---
        const INITIAL_USER = {
            name: "Ren Jiaxuan",
            avatar: "1.jpg", 
            adminPassword: "ralvin", 
            email: "1627716641@qq.com",
            phone: "195 4899 4021",
            school: "浙江师范大学 | 数字媒体艺术",
            gpa: "3.92 (Top 3%)",
            english: "CET-6 550+ | 雅思 6.5",
            exchange: "延世大学 (2025 Summer)",
            resumeFile: "任_佳煊_125515171091.pdf",
            intro: "你好，我是任佳煊。一名追求“策创合一”的数字媒体艺术学生。我不仅擅长用画面讲故事（视频/设计），更热衷于挖掘品牌背后的策略支点。我拥有敏锐的 Z 世代洞察力，期待用灵感与逻辑为品牌创造价值。",
            // 新增可编辑字段
            skillsStrategy: ['品牌全案', '文案策划', '创意脚本', '竞品分析', '社媒运营'],
            skillsVisual: ['PS', 'AI', 'Premiere', 'After Effects', 'C4D', 'Midjourney', 'Figma'],
            honors: [
                { title: "全国一等奖", desc: "全国商科精英挑战赛会展策划赛道" },
                { title: "全国三等奖", desc: "第17届全国大学生广告艺术大赛 (视频类)" },
                { title: "官方参赛认证", desc: "L'Oréal Brandstorm 2025" },
                { title: "省级一等奖", desc: "第13届浙江省大学生广告创意设计大赛" }
            ]
        };

        const INITIAL_PROJECTS = [
            // Strategy
            { id: 's1', mainCat: 'Strategy', subCat: '品牌全案', title: "赤尾：顺滑到位", file: "zioxx-video.mp4", cover: "zioxx-video-cover.png", videoSrc: "zioxx-video.mp4", desc: "摒弃传统说教，用“双人舞步”隐喻亲密关系。设计“丝滑换装”转场。", tags: ["大广赛三等奖", "视频策划"] },
            { id: 's2', mainCat: 'Strategy', subCat: '商赛', title: "L'Oréal Brandstorm", file: "L‘Orealbrainstorm video.mp4", cover: "loreal_cover.jpg", videoSrc: "L‘Orealbrainstorm video.mp4", desc: "提出 TerraGuard 智能剃须生态系统。负责全英文 Pitch Deck。", tags: ["官方认证", "CSR策略"] },
            { id: 's3', mainCat: 'Strategy', subCat: '品牌全案', title: "赤尾品牌手册", file: "zioxx-pitch_cover.png", cover: "zioxx-pitch_cover.png", desc: "针对 Z 世代设计的品牌手册封面。", tags: ["品牌设计", "Z世代"] },
            // Art
            { id: 'a1', mainCat: 'Art', subCat: '影像', title: "Pop Mart：逃离拥挤", file: "pop mart-TVC.mp4", cover: "popmart_cover.jpg", videoSrc: "pop mart-TVC.mp4", desc: "洞察都市焦虑，以耳机为媒介，通过镜头语言完成现实与幻想的切换。", tags: ["浙江省三等奖", "情绪叙事"] },
            { id: 'a2', mainCat: 'Art', subCat: '影像', title: "Are You Lost", file: "are you lost.mp4", cover: "lost_cover.jpg", videoSrc: "are you lost.mp4", desc: "一部探索自我迷失与寻找的实验短片。", tags: ["实验影像", "拼贴动画"] },
            { id: 'a3', mainCat: 'Art', subCat: '影像', title: "养元青：大风吹不去", file: "Yangyuanqing-video.mp4", cover: "yang_cover.jpg", videoSrc: "Yangyuanqing-video.mp4", desc: "运用视觉通感，将“发根”比作“墙上海报”。", tags: ["视觉隐喻", "定格动画"] },
            { id: 'a4', mainCat: 'Art', subCat: '文创设计', title: "扬州文人茶", file: "Yangzhoutea2.jpg", cover: "Yangzhoutea2.jpg", desc: "挖掘“扬州八怪”文化IP，提炼水墨元素，确立“新中式文人茶”调性。", tags: ["包装设计", "国潮"] },
            { id: 'a5', mainCat: 'Art', subCat: '插画', title: "客家食养", file: "hakka1.jpg", cover: "hakka1.jpg", desc: "基于客家“顺时而食”智慧，将特色美食与节气结合。", tags: ["插画设计", "文创"] },
            { id: 'a6', mainCat: 'Art', subCat: '海报', title: "营养快线海报", file: "nutri-express-poster1.jpg", cover: "nutri-express-poster1.jpg", desc: "利用 C4D 构建超现实场景，生动展现产品核心卖点。", tags: ["C4D", "海报"] },
            { id: 'a7', mainCat: 'Art', subCat: 'AIGC', title: "器·观：AIGC系列", file: "grassland-liver.jpg", cover: "grassland-liver.jpg", desc: "利用 AIGC 将人体器官创意化为自然地貌。", tags: ["Midjourney", "创意合成"] }
        ];

        const INITIAL_EVENTS = {
            "2025-05-28": { title: "欧莱雅商赛", type: "competition", desc: "完成了全英文 Pitch！虽然过程很艰难，熬了几个通宵改 Deck，但看到 TerraGuard 概念成型的那一刻，值了。✨", image: null },
            "2025-06-10": { title: "大广赛获奖", type: "award", desc: "赤尾视频拿到了全国三等奖！🎬 评委说“舞蹈隐喻”的点子很妙。这坚定了我做内容创意的决心。", image: null },
            "2025-09-25": { title: "AIGC 探索日", type: "creation", desc: "第一次用 MJ 跑出了完美的“器官山水”图。科技与艺术的边界正在消融，太迷人了。🎨", image: null },
            "2025-05-04": { title: "My Birthday", type: "life", desc: "祝我生日快乐！🎂 许愿能进入喜欢的品牌做策划。", image: null }
        };

        const APPS = [
            { id: 'resume', name: 'Resume', icon: 'fa-regular fa-address-card', color: 'bg-blue-500', title: 'My Profile' },
            { id: 'portfolio', name: 'Portfolio', icon: 'fa-regular fa-images', color: 'bg-gradient-to-br from-purple-500 to-pink-500', title: 'Works Gallery' },
            { id: 'calendar', name: 'Calendar', icon: 'fa-regular fa-calendar', color: 'bg-white text-red-500', title: 'Life Log' }, 
            { id: 'mail', name: 'Contact', icon: 'fa-regular fa-envelope', color: 'bg-green-500', title: 'Contact Me' },
        ];

        // --- Generic Components ---
        const GenericIcon = ({ app }) => (
            <div className={`w-full h-full ${app.color} rounded-[22%] flex items-center justify-center text-white app-icon relative overflow-hidden border border-white/10 shadow-md`}>
                {app.id === 'calendar' ? (
                    <div className="w-full h-full bg-white flex flex-col items-center">
                        <div className="h-[25%] w-full bg-red-500"></div>
                        <div className="flex-1 flex flex-col items-center justify-center">
                            <span className="text-black font-bold text-2xl leading-none">{new Date().getDate()}</span>
                        </div>
                    </div>
                ) : (
                    <>
                        <div className="absolute top-0 left-0 w-full h-full bg-gradient-to-br from-white/30 to-transparent pointer-events-none"></div>
                        <i className={`${app.icon} text-3xl ${app.id === 'calendar' ? 'text-red-500' : 'text-white'}`}></i>
                    </>
                )}
            </div>
        );

        // --- Resume App (Finder) - Fully Editable ---
        const ResumeApp = ({ isAdmin, user, updateUser }) => {
            const [isEditing, setIsEditing] = useState(false);
            const [editForm, setEditForm] = useState(user);

            useEffect(() => { setEditForm(user); }, [user]);

            const handleSave = () => {
                updateUser(editForm);
                setIsEditing(false);
            };

            const handleChange = (field, value) => setEditForm({ ...editForm, [field]: value });

            // Handle array change (split by comma)
            const handleArrayChange = (field, value) => {
                const arr = value.split(',').map(item => item.trim());
                setEditForm({ ...editForm, [field]: arr });
            };

            // Handle Honor change
            const handleHonorChange = (index, key, value) => {
                const newHonors = [...editForm.honors];
                newHonors[index][key] = value;
                setEditForm({ ...editForm, honors: newHonors });
            };

            return (
                <div className="flex h-full bg-[#F5F5F5] relative">
                    {isAdmin && (
                        <button 
                            onClick={() => isEditing ? handleSave() : setIsEditing(true)} 
                            className="absolute top-4 right-4 z-10 px-3 py-1 bg-blue-500 text-white rounded text-xs hover:bg-blue-600 shadow-sm"
                        >
                            {isEditing ? 'Save' : 'Edit Profile'}
                        </button>
                    )}

                    <div className="w-72 bg-[#EDECEB] border-r border-gray-300 pt-8 px-6 flex flex-col items-center text-center shrink-0 overflow-y-auto">
                        <div className="w-32 h-32 rounded-full overflow-hidden mb-6 border-4 border-white shadow-lg shrink-0">
                            <img src={user.avatar} className="w-full h-full object-cover object-top" />
                        </div>
                        <h2 className="font-bold text-2xl text-gray-800 mb-1">{user.name}</h2>
                        <p className="text-sm text-gray-500 mb-8">Brand Strategy & Creative</p>
                        
                        <div className="w-full space-y-3 text-left">
                            {/* Info Blocks */}
                            {['school', 'english', 'exchange', 'email'].map(field => (
                                <div key={field} className="bg-white p-3 rounded-lg shadow-sm">
                                    <div className="text-xs text-gray-400 font-bold uppercase mb-1">{field}</div>
                                    {isEditing ? (
                                        <input className="edit-input text-sm" value={editForm[field]} onChange={e => handleChange(field, e.target.value)} />
                                    ) : (
                                        <div className="text-sm font-medium break-words">{user[field]}</div>
                                    )}
                                </div>
                            ))}
                            
                            {!isEditing && (
                                <a href={user.resumeFile} download className="block w-full bg-blue-600 text-white text-center py-2 rounded-lg font-medium text-sm hover:bg-blue-700 transition shadow-sm mt-6">
                                    <i className="fa-solid fa-download mr-2"></i> Download PDF
                                </a>
                            )}
                        </div>
                    </div>
                    <div className="flex-1 p-12 overflow-y-auto bg-white">
                        <h1 className="text-4xl font-bold mb-8">About Me</h1>
                        {isEditing ? (
                            <textarea 
                                className="edit-textarea text-lg text-gray-600 mb-10" 
                                value={editForm.intro} 
                                onChange={e => handleChange('intro', e.target.value)} 
                            />
                        ) : (
                            <p className="text-gray-600 leading-relaxed mb-10 text-lg max-w-2xl">{user.intro}</p>
                        )}
                        
                        <div className="grid grid-cols-1 md:grid-cols-2 gap-12">
                             {/* Skills Editable */}
                             <div>
                                <h3 className="font-bold text-gray-800 border-b-2 border-black pb-2 mb-4 text-lg">Professional Skills</h3>
                                <div className="space-y-4">
                                    <div>
                                        <div className="text-sm font-bold mb-1">Strategy & Content</div>
                                        {isEditing ? (
                                            <textarea className="edit-textarea" value={editForm.skillsStrategy.join(', ')} onChange={e => handleArrayChange('skillsStrategy', e.target.value)} />
                                        ) : (
                                            <div className="flex flex-wrap gap-2">
                                                {user.skillsStrategy.map(s=><span key={s} className="px-3 py-1 bg-gray-100 rounded text-sm text-gray-700">{s}</span>)}
                                            </div>
                                        )}
                                    </div>
                                    <div>
                                        <div className="text-sm font-bold mb-1">Visual & Tools</div>
                                        {isEditing ? (
                                            <textarea className="edit-textarea" value={editForm.skillsVisual.join(', ')} onChange={e => handleArrayChange('skillsVisual', e.target.value)} />
                                        ) : (
                                            <div className="flex flex-wrap gap-2">
                                                {user.skillsVisual.map(s=><span key={s} className="px-3 py-1 bg-gray-100 rounded text-sm text-gray-700">{s}</span>)}
                                            </div>
                                        )}
                                    </div>
                                </div>
                            </div>

                            {/* Honors Editable */}
                            <div>
                                <h3 className="font-bold text-gray-800 border-b-2 border-black pb-2 mb-4 text-lg">Honors</h3>
                                <ul className="space-y-4 text-gray-700">
                                    {editForm.honors.map((h, idx) => (
                                        <li key={idx} className="flex items-start border-b border-gray-100 pb-2">
                                            <i className="fa-solid fa-trophy text-yellow-500 mt-1 mr-3"></i>
                                            <div className="w-full">
                                                {isEditing ? (
                                                    <>
                                                        <input className="edit-input font-bold mb-1" value={h.title} onChange={e => handleHonorChange(idx, 'title', e.target.value)} />
                                                        <input className="edit-input text-sm" value={h.desc} onChange={e => handleHonorChange(idx, 'desc', e.target.value)} />
                                                    </>
                                                ) : (
                                                    <>
                                                        <div className="font-bold">{h.title}</div>
                                                        <div className="text-sm text-gray-500">{h.desc}</div>
                                                    </>
                                                )}
                                            </div>
                                        </li>
                                    ))}
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            );
        };

        // --- Portfolio App (CMS with Tags Editing) ---
        const PortfolioApp = ({ isAdmin, projects, updateProjects }) => {
            const [mainCat, setMainCat] = useState('All');
            const [subCat, setSubCat] = useState('All');
            const [selectedProject, setSelectedProject] = useState(null);
            
            const [isEditingMode, setIsEditingMode] = useState(false);
            const [editProject, setEditProject] = useState(null);
            const fileInputRef = useRef(null);

            const filteredProjects = projects.filter(p => {
                if (mainCat === 'All') return true;
                if (p.mainCat !== mainCat) return false;
                if (subCat === 'All') return true;
                return p.subCat === subCat;
            });

            const activeProject = selectedProject || filteredProjects[0];

            const handleSaveProject = () => {
                let newProjects;
                if (editProject.id === 'new') {
                    const newId = 'p' + Date.now();
                    newProjects = [...projects, { ...editProject, id: newId }];
                } else {
                    newProjects = projects.map(p => p.id === editProject.id ? editProject : p);
                }
                updateProjects(newProjects);
                setIsEditingMode(false); 
                setEditProject(null);
            };

            const handleAddNew = () => {
                setEditProject({ id: 'new', mainCat: 'Strategy', subCat: 'General', title: 'New Project', desc: 'Description...', tags: ['Tag'], file: '1.jpg', cover: '1.jpg' });
                setIsEditingMode(true);
            };

            const handleEditExisting = (p) => { setEditProject({ ...p }); setIsEditingMode(true); };
            const handleImageUpload = (e) => { const file = e.target.files[0]; if (file) { const reader = new FileReader(); reader.onloadend = () => { setEditProject({ ...editProject, cover: reader.result, file: reader.result }); }; reader.readAsDataURL(file); } };

            // Helper to handle tags editing
            const handleTagsChange = (value) => {
                const tagsArray = value.split(',').map(t => t.trim()).filter(t => t);
                setEditProject({ ...editProject, tags: tagsArray });
            };

            const renderSidebar = () => (
                <div className="w-64 bg-[#F3F2F2] border-r border-[#E5E5E5] pt-4 flex flex-col backdrop-blur-xl h-full shrink-0 overflow-y-auto relative">
                     {isAdmin && <div className="px-4 mb-4"><button onClick={handleAddNew} className="w-full bg-blue-500 text-white py-1 rounded text-xs font-bold hover:bg-blue-600 transition shadow-sm">+ New Project</button></div>}
                    <div onClick={() => { setMainCat('All'); setSubCat('All'); setSelectedProject(null); }} className={`px-4 py-2 mx-2 rounded-md text-sm cursor-pointer flex items-center gap-3 ${mainCat==='All' ? 'bg-[#DAD9D9] font-bold' : 'hover:bg-[#E5E5E5]'}`}><i className="fa-solid fa-layer-group text-blue-500"></i> All Works</div>
                    <div className="mt-6 px-4 mb-1 text-xs font-bold text-gray-400 uppercase">Brand Strategy</div>
                    {['品牌全案', '视频策划', '商赛'].map(sub => <div key={sub} onClick={() => { setMainCat('Strategy'); setSubCat(sub); setSelectedProject(null); }} className={`px-4 py-1.5 mx-2 pl-10 rounded-md text-sm cursor-pointer ${mainCat==='Strategy' && subCat===sub ? 'bg-[#DAD9D9]' : 'hover:bg-[#E5E5E5]'}`}>{sub}</div>)}
                    <div className="mt-6 px-4 mb-1 text-xs font-bold text-gray-400 uppercase">Artistic Expression</div>
                    {['影像', '插画', '海报', '文创设计', 'AIGC'].map(sub => <div key={sub} onClick={() => { setMainCat('Art'); setSubCat(sub); setSelectedProject(null); }} className={`px-4 py-1.5 mx-2 pl-10 rounded-md text-sm cursor-pointer ${mainCat==='Art' && subCat===sub ? 'bg-[#DAD9D9]' : 'hover:bg-[#E5E5E5]'}`}>{sub}</div>)}
                </div>
            );

            if (isEditingMode && editProject) {
                return (
                    <div className="flex h-full bg-white p-8 flex-col overflow-y-auto relative">
                        <h2 className="text-2xl font-bold mb-6">{editProject.id === 'new' ? 'Add New Project' : 'Edit Project'}</h2>
                        <div className="space-y-4 max-w-xl">
                            <div><span className="edit-label">Title</span><input className="edit-input" value={editProject.title} onChange={e => setEditProject({...editProject, title: e.target.value})} /></div>
                            <div className="grid grid-cols-2 gap-4">
                                <div><span className="edit-label">Main Category</span><select className="edit-input" value={editProject.mainCat} onChange={e => setEditProject({...editProject, mainCat: e.target.value})}><option value="Strategy">Strategy</option><option value="Art">Art</option></select></div>
                                <div><span className="edit-label">Sub Category</span><input className="edit-input" value={editProject.subCat} onChange={e => setEditProject({...editProject, subCat: e.target.value})} /></div>
                            </div>
                            <div><span className="edit-label">Description</span><textarea className="edit-textarea" value={editProject.desc} onChange={e => setEditProject({...editProject, desc: e.target.value})} /></div>
                            
                            {/* Tags Editing */}
                            <div><span className="edit-label">Tags (Comma separated)</span><input className="edit-input" value={editProject.tags.join(', ')} onChange={e => handleTagsChange(e.target.value)} /></div>

                             <div><span className="edit-label">Cover Image (Click to Upload)</span><div className="h-24 border-2 border-dashed border-gray-300 rounded flex items-center justify-center cursor-pointer hover:bg-gray-50" onClick={() => fileInputRef.current.click()}>{editProject.cover && editProject.cover.length > 20 ? <img src={editProject.cover} className="h-full object-contain"/> : <span className="text-sm text-gray-400">{editProject.cover || 'Click to Upload'}</span>}</div><input type="file" ref={fileInputRef} className="hidden" onChange={handleImageUpload} /></div>
                            <div className="flex gap-3 pt-4"><button onClick={handleSaveProject} className="bg-blue-500 text-white px-6 py-2 rounded hover:bg-blue-600 shadow-md">Save Project</button><button onClick={() => setIsEditingMode(false)} className="text-gray-500 px-4 py-2 hover:bg-gray-100 rounded">Cancel</button></div>
                        </div>
                    </div>
                )
            }

            return (
                <div className="flex h-full bg-white text-gray-800">
                    {renderSidebar()}
                    <div className="flex-1 flex flex-col h-full overflow-hidden">
                        <div className="flex-1 bg-[#FAFAFA] flex items-center justify-center p-8 border-b border-[#E5E5E5] overflow-hidden relative group">
                            {activeProject ? (
                                (activeProject.videoSrc) ? (
                                    <video src={activeProject.videoSrc} controls className="max-w-full max-h-full rounded-lg shadow-lg" poster={activeProject.cover}></video>
                                ) : (
                                    <img src={activeProject.cover || activeProject.file} className="max-w-full max-h-full rounded-lg shadow-lg object-contain" />
                                )
                            ) : <div className="text-gray-400">Select an item</div>}
                        </div>
                        <div className="h-40 bg-white overflow-x-auto whitespace-nowrap p-4 flex gap-4 items-center shrink-0">
                            {filteredProjects.map(p => (
                                <div key={p.id} onClick={() => setSelectedProject(p)} className={`inline-block w-32 text-center cursor-pointer p-2 rounded-md transition-all ${activeProject && activeProject.id === p.id ? 'bg-[#E5F1FB]' : 'hover:bg-[#F5F5F5]'}`}>
                                    <div className="w-24 h-20 mx-auto bg-gray-200 rounded-md overflow-hidden mb-2 relative border border-gray-300">
                                        <img src={p.cover || p.file} className="w-full h-full object-cover" />
                                        {p.videoSrc && <div className="absolute inset-0 flex items-center justify-center bg-black/20"><i className="fa-solid fa-play text-white drop-shadow-md"></i></div>}
                                    </div>
                                    <div className="text-xs text-gray-700 truncate w-full px-1 font-medium">{p.title}</div>
                                </div>
                            ))}
                        </div>
                    </div>
                    <div className="w-72 bg-white border-l border-[#E5E5E5] p-6 overflow-y-auto hidden md:block shrink-0 relative">
                        {activeProject ? (
                            <>
                                {isAdmin && <button onClick={() => handleEditExisting(activeProject)} className="absolute top-4 right-4 text-blue-500 hover:bg-blue-50 p-2 rounded"><i className="fa-solid fa-pen"></i></button>}
                                <h3 className="font-bold text-gray-800 text-lg mb-1 pr-8">{activeProject.title}</h3>
                                <div className="text-xs text-blue-600 font-medium mb-6 bg-blue-50 inline-block px-2 py-1 rounded">{activeProject.mainCat} / {activeProject.subCat}</div>
                                <div className="space-y-6">
                                    <div><div className="text-[10px] font-bold text-gray-400 uppercase mb-2">Description</div><p className="text-sm text-gray-600 leading-relaxed">{activeProject.desc}</p></div>
                                    <div><div className="text-[10px] font-bold text-gray-400 uppercase mb-2">Tags</div><div className="flex flex-wrap gap-1">{activeProject.tags.map(t => <span key={t} className="px-2 py-1 bg-gray-100 text-gray-600 rounded text-xs">#{t}</span>)}</div></div>
                                </div>
                            </>
                        ) : <div className="h-full flex items-center justify-center text-xs text-gray-400">No selection</div>}
                    </div>
                </div>
            );
        };

        // --- Calendar App (Full Featured - Year & Month Nav) ---
        const CalendarApp = ({ isAdmin, events, updateEvents }) => {
            const [currentDate, setCurrentDate] = useState(new Date());
            const [selectedDate, setSelectedDate] = useState(new Date().toISOString().split('T')[0]);
            const [isEditing, setIsEditing] = useState(false);
            
            const [editTitle, setEditTitle] = useState('');
            const [editDesc, setEditDesc] = useState('');
            const [editImage, setEditImage] = useState(null);
            const fileInputRef = useRef(null);

            const getMonthData = (date) => {
                const year = date.getFullYear();
                const month = date.getMonth();
                const firstDay = new Date(year, month, 1).getDay();
                const daysInMonth = new Date(year, month + 1, 0).getDate();
                let days = [];
                for(let i=0; i<firstDay; i++) days.push(null);
                for(let i=1; i<=daysInMonth; i++) {
                    const dStr = `${year}-${String(month+1).padStart(2,'0')}-${String(i).padStart(2,'0')}`;
                    days.push({ day: i, fullDate: dStr, hasEvent: events[dStr] });
                }
                return days;
            };
            const days = getMonthData(currentDate);
            const currentEvent = events[selectedDate];

            const changeMonth = (delta) => setCurrentDate(new Date(currentDate.getFullYear(), currentDate.getMonth() + delta, 1));
            const changeYear = (delta) => setCurrentDate(new Date(currentDate.getFullYear() + delta, currentDate.getMonth(), 1));

            useEffect(() => {
                setIsEditing(false);
                if (currentEvent) {
                    setEditTitle(currentEvent.title); setEditDesc(currentEvent.desc); setEditImage(currentEvent.image);
                } else {
                    setEditTitle(''); setEditDesc(''); setEditImage(null);
                }
            }, [selectedDate, events]);

            const handleSave = () => {
                const newEvents = { ...events, [selectedDate]: { title: editTitle || "New Memory", type: "life", desc: editDesc, image: editImage } };
                updateEvents(newEvents);
                setIsEditing(false);
            };
             
            const handleImageUpload = (e) => {
                const file = e.target.files[0];
                if (file) {
                    const reader = new FileReader();
                    reader.onloadend = () => setEditImage(reader.result);
                    reader.readAsDataURL(file);
                }
            };

            return (
                <div className="flex h-full bg-white">
                    <div className="flex-1 p-8 flex flex-col border-r border-gray-200 overflow-y-auto">
                        <div className="flex justify-between items-center mb-2">
                             <button onClick={() => changeYear(-1)} className="text-xs text-gray-400 hover:text-blue-500 font-bold">{currentDate.getFullYear()-1}</button>
                             <span className="text-sm font-bold text-gray-400 tracking-wider">{currentDate.getFullYear()}</span>
                             <button onClick={() => changeYear(1)} className="text-xs text-gray-400 hover:text-blue-500 font-bold">{currentDate.getFullYear()+1}</button>
                        </div>
                        <div className="flex justify-between items-center mb-6">
                            <button onClick={() => changeMonth(-1)} className="p-2 hover:bg-gray-100 rounded"><i className="fa-solid fa-chevron-left"></i></button>
                            <h2 className="text-xl font-bold text-red-500">{currentDate.toLocaleDateString('en-US', { month: 'long' })}</h2>
                            <button onClick={() => changeMonth(1)} className="p-2 hover:bg-gray-100 rounded"><i className="fa-solid fa-chevron-right"></i></button>
                        </div>
                        <div className="grid grid-cols-7 mb-2 text-xs font-bold text-gray-400 uppercase text-center"><span>Sun</span><span>Mon</span><span>Tue</span><span>Wed</span><span>Thu</span><span>Fri</span><span>Sat</span></div>
                        <div className="grid grid-cols-7 gap-1 bg-gray-200 border border-gray-200 auto-rows-fr">
                            {days.map((day, idx) => (
                                <div key={idx} onClick={() => day && setSelectedDate(day.fullDate)} className={`bg-white min-h-[80px] p-2 cursor-pointer transition hover:bg-gray-50 relative ${!day ? 'bg-gray-50 cursor-default' : ''} ${day && selectedDate === day.fullDate ? 'ring-2 ring-inset ring-blue-400 z-10' : ''}`}>
                                    {day && <><span className={`inline-block w-6 h-6 text-center leading-6 rounded-full text-sm ${day.fullDate === new Date().toISOString().split('T')[0] ? 'bg-red-500 text-white' : ''}`}>{day.day}</span>{day.hasEvent && <div className="mt-1"><div className="w-1.5 h-1.5 rounded-full bg-blue-500 mb-1"></div><div className="text-[10px] text-gray-500 truncate leading-tight">{day.hasEvent.title}</div></div>}</>}
                                </div>
                            ))}
                        </div>
                    </div>
                    <div className="w-96 bg-[#F9FAFB] p-8 flex flex-col overflow-y-auto shrink-0">
                        <div className="text-2xl font-light text-gray-400 mb-6 border-b pb-4">{selectedDate}</div>
                        {isEditing ? (
                            <div className="flex flex-col gap-4 h-full animate-fade-in">
                                <input className="text-xl font-bold bg-white border border-gray-300 rounded px-3 py-2" placeholder="Title..." value={editTitle} onChange={e => setEditTitle(e.target.value)} />
                                <textarea className="flex-1 bg-white border border-gray-300 rounded px-3 py-2 text-sm leading-relaxed resize-none" placeholder="Write something..." value={editDesc} onChange={e => setEditDesc(e.target.value)}></textarea>
                                <div className="h-32 border-2 border-dashed border-gray-300 rounded flex flex-col items-center justify-center cursor-pointer hover:bg-gray-100 relative overflow-hidden" onClick={() => fileInputRef.current.click()}>
                                    {editImage ? <img src={editImage} className="w-full h-full object-cover" /> : <span className="text-gray-400 text-sm"><i className="fa-solid fa-image mr-2"></i> Add Photo</span>}
                                    <input type="file" ref={fileInputRef} onChange={handleImageUpload} className="hidden" accept="image/*" />
                                </div>
                                <div className="flex gap-2 mt-auto"><button onClick={handleSave} className="flex-1 bg-blue-500 text-white py-2 rounded hover:bg-blue-600 transition">Save</button><button onClick={() => setIsEditing(false)} className="px-4 py-2 text-gray-500 hover:text-gray-700">Cancel</button></div>
                            </div>
                        ) : (
                            <>
                                {currentEvent ? (
                                    <div className="animate-fade-in h-full flex flex-col">
                                        <h3 className="text-2xl font-bold mb-4 text-gray-800">{currentEvent.title}</h3>
                                        {currentEvent.image && <div className="w-full h-48 rounded-lg overflow-hidden mb-4 shadow-sm border border-gray-200"><img src={currentEvent.image} className="w-full h-full object-cover" /></div>}
                                        <div className="text-gray-600 leading-relaxed text-sm whitespace-pre-wrap bg-white p-4 rounded-lg shadow-sm border border-gray-200 flex-1">{currentEvent.desc}</div>
                                        {isAdmin && <button onClick={() => setIsEditing(true)} className="mt-6 w-full border border-gray-300 text-gray-600 py-2 rounded hover:bg-white transition">Edit Entry</button>}
                                    </div>
                                ) : (
                                    <div className="flex-1 flex flex-col items-center justify-center text-center text-gray-400">
                                        <div className="text-4xl mb-4">🍃</div>
                                        <p className="text-sm">No memories here yet.</p>
                                        {isAdmin && <button onClick={() => setIsEditing(true)} className="mt-6 px-6 py-2 bg-blue-500 text-white rounded-full hover:bg-blue-600 transition text-sm">+ Add New Entry</button>}
                                    </div>
                                )}
                            </>
                        )}
                    </div>
                </div>
            );
        };

        // --- Contact (Mail) ---
        const MailApp = ({ user }) => (
            <div className="h-full flex flex-col items-center justify-center bg-white p-8 text-center">
                <div className="w-20 h-20 bg-green-500 rounded-full flex items-center justify-center text-white text-4xl mb-6 shadow-lg"><i className="fa-regular fa-comment-dots"></i></div>
                <h2 className="text-2xl font-bold mb-2">Let's Chat!</h2>
                <p className="text-gray-500 mb-8">期待与您共创下一个品牌高光时刻。</p>
                <div className="space-y-4 w-full max-w-xs">
                    <a href={`tel:${user.phone}`} className="block p-4 rounded-xl bg-gray-50 hover:bg-gray-100 transition flex items-center gap-4 border border-gray-100"><div className="w-10 h-10 rounded-full bg-green-100 text-green-600 flex items-center justify-center"><i className="fa-solid fa-phone"></i></div><div className="text-left"><div className="text-xs text-gray-400 font-bold uppercase">Phone</div><div className="font-medium">{user.phone}</div></div></a>
                    <a href={`mailto:${user.email}`} className="block p-4 rounded-xl bg-gray-50 hover:bg-gray-100 transition flex items-center gap-4 border border-gray-100"><div className="w-10 h-10 rounded-full bg-blue-100 text-blue-600 flex items-center justify-center"><i className="fa-solid fa-envelope"></i></div><div className="text-left"><div className="text-xs text-gray-400 font-bold uppercase">Email</div><div className="font-medium">{user.email}</div></div></a>
                </div>
            </div>
        );

        // --- OS Logic ---
        const MacOS = () => {
            const [locked, setLocked] = useState(true);
            const [booting, setBooting] = useState(false);
            const [currentTime, setCurrentTime] = useState(new Date());
            const [windows, setWindows] = useState([]);
            const [activeWindowId, setActiveWindowId] = useState(null);
            const [isAdmin, setIsAdmin] = useState(false); 
            const [passwordInput, setPasswordInput] = useState("");
            const [errorMsg, setErrorMsg] = useState("");

            // 全局状态管理 (Lifted State)
            const [userData, setUserData] = useState(() => JSON.parse(localStorage.getItem('macOS_User')) || INITIAL_USER);
            const [projectData, setProjectData] = useState(() => JSON.parse(localStorage.getItem('macOS_Projects')) || INITIAL_PROJECTS);
            const [eventData, setEventData] = useState(() => JSON.parse(localStorage.getItem('macOS_Calendar_Events')) || INITIAL_EVENTS);

            // 数据更新函数
            const updateUser = (newData) => { setUserData(newData); localStorage.setItem('macOS_User', JSON.stringify(newData)); };
            const updateProjects = (newData) => { setProjectData(newData); localStorage.setItem('macOS_Projects', JSON.stringify(newData)); };
            const updateEvents = (newData) => { setEventData(newData); localStorage.setItem('macOS_Calendar_Events', JSON.stringify(newData)); };
            
            // 清除/重置系统 (新增功能，方便调试)
            const handleSystemReset = () => {
                if(confirm("确定要重置系统吗？所有修改将丢失。")) {
                    localStorage.clear();
                    window.location.reload();
                }
            };

            const dragItem = useRef(); const dragStartPos = useRef(); const resizeItem = useRef();

            useEffect(() => { const t = setInterval(() => setCurrentTime(new Date()), 1000); return () => clearInterval(t); }, []);
            
            const timeString = currentTime.toLocaleTimeString('en-US', { hour: 'numeric', minute: '2-digit', hour12: false });
            const dateString = currentTime.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' });

            const handleLogin = () => {
                if (passwordInput === userData.adminPassword) {
                    setBooting(true);
                    setTimeout(() => { setLocked(false); setIsAdmin(true); openApp('resume'); }, 800);
                } else if (passwordInput === "") {
                    setBooting(true);
                    setTimeout(() => { setLocked(false); setIsAdmin(false); openApp('resume'); }, 800);
                } else {
                    setErrorMsg("Incorrect password. Leave empty for Guest Mode.");
                }
            };
            
            // Logout
            const handleLogout = () => {
                 setWindows([]);
                 setLocked(true);
                 setBooting(false);
                 setPasswordInput("");
            };

            const openApp = (appId) => {
                const app = APPS.find(a => a.id === appId);
                if (!app) return;
                if (windows.find(w => w.id === appId)) {
                    setWindows(prev => prev.map(w => w.id === appId ? { ...w, minimized: false, zIndex: Date.now() } : w));
                    setActiveWindowId(appId);
                } else {
                    const width = 1000; const height = 650;
                    const newWin = { id: appId, title: app.title, app: app, x: Math.max(20, (window.innerWidth - width) / 2), y: Math.max(40, (window.innerHeight - height) / 2), width, height, zIndex: Date.now(), minimized: false, maximized: false, isEditingTitle: false };
                    setWindows([...windows, newWin]);
                    setActiveWindowId(appId);
                }
            };

            const closeWindow = (id) => setWindows(windows.filter(w => w.id !== id));
            const minimizeWindow = (id) => setWindows(windows.map(w => w.id === id ? { ...w, minimized: true } : w));
            const focusWindow = (id) => { setActiveWindowId(id); setWindows(windows.map(w => w.id === id ? { ...w, zIndex: Date.now() } : w)); };
            const toggleMaximize = (id) => setWindows(windows.map(w => w.id === id ? { ...w, maximized: !w.maximized, x: !w.maximized ? 0 : 50, y: !w.maximized ? 32 : 50, width: !w.maximized ? window.innerWidth : 1000, height: !w.maximized ? window.innerHeight - 80 : 650 } : w));

            const onTitleMouseDown = (e, id) => {
                focusWindow(id);
                const win = windows.find(w => w.id === id);
                if(win.maximized) return;
                dragItem.current = id;
                dragStartPos.current = { mouseX: e.clientX, mouseY: e.clientY, winX: win.x, winY: win.y };
                document.addEventListener('mousemove', onMouseMove);
                document.addEventListener('mouseup', onMouseUp);
            };
            const onResizeMouseDown = (e, id) => {
                e.stopPropagation();
                resizeItem.current = id;
                const win = windows.find(w => w.id === id);
                dragStartPos.current = { mouseX: e.clientX, mouseY: e.clientY, w: win.width, h: win.height };
                document.addEventListener('mousemove', onMouseMove);
                document.addEventListener('mouseup', onMouseUp);
            };
            const onMouseMove = (e) => {
                if (dragItem.current) {
                    e.preventDefault();
                    const dx = e.clientX - dragStartPos.current.mouseX;
                    const dy = e.clientY - dragStartPos.current.mouseY;
                    setWindows(prev => prev.map(w => w.id === dragItem.current ? { ...w, x: dragStartPos.current.winX + dx, y: dragStartPos.current.winY + dy } : w));
                } else if (resizeItem.current) {
                    e.preventDefault();
                    const dx = e.clientX - dragStartPos.current.mouseX;
                    const dy = e.clientY - dragStartPos.current.mouseY;
                    setWindows(prev => prev.map(w => w.id === resizeItem.current ? { ...w, width: Math.max(600, dragStartPos.current.w + dx), height: Math.max(400, dragStartPos.current.h + dy) } : w));
                }
            };
            const onMouseUp = () => { dragItem.current = null; resizeItem.current = null; document.removeEventListener('mousemove', onMouseMove); document.removeEventListener('mouseup', onMouseUp); };

            // Desktop Icon Drag (Admin Only)
            const [desktopIcons, setDesktopIcons] = useState(APPS.map((app, i) => ({ ...app, x: null, y: null })));
            const iconDragItem = useRef();
            
            const onIconMouseDown = (e, index) => {
                if (!isAdmin) return;
                iconDragItem.current = index;
                dragStartPos.current = { mouseX: e.clientX, mouseY: e.clientY };
                document.addEventListener('mousemove', onIconMouseMove);
                document.addEventListener('mouseup', onIconMouseUp);
            };
            const onIconMouseMove = (e) => {
                if (iconDragItem.current === undefined) return;
                // Simplified logic: just visual drag for now, complex grid logic omitted for brevity
            };
            const onIconMouseUp = () => { iconDragItem.current = undefined; document.removeEventListener('mousemove', onIconMouseMove); document.removeEventListener('mouseup', onIconMouseUp); };


            if (locked) {
                return (
                    <div className="w-full h-screen lockscreen-bg flex flex-col items-center pt-20 transition-transform duration-700 ease-in-out" style={{ transform: booting ? 'translateY(-100%)' : 'translateY(0)' }}>
                        <div className="text-white text-center drop-shadow-xl mb-12">
                            <div className="text-[100px] font-thin leading-none tracking-tight font-[Inter]">{timeString}</div>
                            <div className="text-2xl font-normal opacity-90">{dateString}</div>
                        </div>
                        <div className="flex flex-col items-center animate-fade-in mt-10">
                            <img src={userData.avatar} className="w-24 h-24 rounded-full mb-6 shadow-2xl object-cover object-top ring-2 ring-white/20" />
                            <h2 className="text-white text-xl font-semibold mb-6 drop-shadow-md tracking-wide">{userData.name}</h2>
                            <div className="relative group flex flex-col items-center gap-2">
                                <div className="relative">
                                    <input type="password" placeholder="Enter Password" value={passwordInput} onChange={(e)=>setPasswordInput(e.target.value)} className="w-48 bg-white/20 border-0 rounded-full px-4 py-1.5 text-white text-center backdrop-blur-md transition-all focus:bg-white/30 focus:w-56" onKeyDown={(e) => e.key === 'Enter' && handleLogin()} />
                                    <button onClick={handleLogin} className="absolute right-2 top-1/2 -translate-y-1/2 text-white/70 hover:text-white transition"><i className="fa-solid fa-arrow-right"></i></button>
                                </div>
                                {errorMsg && <div className="text-red-300 text-xs">{errorMsg}</div>}
                                <p className="text-white/40 text-[10px] mt-2 font-light tracking-widest uppercase cursor-pointer hover:text-white/80" onClick={() => { setPasswordInput(""); handleLogin(); }}>Or Click for Guest Mode</p>
                            </div>
                        </div>
                    </div>
                );
            }

            return (
                <div className="w-full h-screen wallpaper overflow-hidden relative font-[Inter]">
                    <div className="h-7 w-full glass-dark flex items-center justify-between px-4 text-xs font-medium z-50 absolute top-0 shadow-sm text-white/90">
                        <div className="flex items-center gap-4">
                            <i className="fa-brands fa-apple text-sm cursor-pointer hover:text-white" onClick={handleLogout}></i>
                            <span className="font-bold">Portfolio</span>
                            <span className="cursor-pointer hover:text-white" onClick={handleSystemReset}>Reset System</span>
                        </div>
                        <div className="flex items-center gap-4">
                            {isAdmin && <span className="bg-blue-500/80 px-2 py-0.5 rounded text-[10px]">Admin</span>}
                            <i className="fa-solid fa-wifi"></i><span>{currentTime.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</span>
                        </div>
                    </div>

                    <div className="absolute top-10 right-4 flex flex-col gap-4 items-end z-0">
                        {desktopIcons.map((app, idx) => (
                            <div key={app.id} onDoubleClick={() => openApp(app.id)} onMouseDown={(e) => onIconMouseDown(e, idx)} className="desktop-icon">
                                <div className={`w-14 h-14 rounded-xl shadow-lg flex items-center justify-center text-3xl mb-1 text-white desktop-icon-img ${app.color} bg-gradient-to-br from-white/20 to-transparent border border-white/20 backdrop-blur-md`}>
                                    {app.id === 'calendar' ? <span className="font-bold text-black">{new Date().getDate()}</span> : <i className={app.icon}></i>}
                                </div>
                                <span className="text-white text-xs font-medium drop-shadow-md bg-black/20 px-2 rounded">{app.name}</span>
                            </div>
                        ))}
                    </div>

                    {windows.map(win => (
                        <div key={win.id} className={`absolute rounded-lg overflow-hidden glass shadow-mac-window flex flex-col animate-scale-up ${win.minimized ? 'hidden' : ''}`} style={{ left: win.x, top: win.y, width: win.width, height: win.height, zIndex: win.zIndex }} onMouseDown={() => focusWindow(win.id)}>
                            <div className="h-8 bg-[#E3E3E3] border-b border-[#D1D1D1] flex items-center px-3 gap-2 backdrop-blur-xl" onMouseDown={(e) => onTitleMouseDown(e, win.id)}>
                                <div className="flex gap-2">
                                    <div onClick={(e) => {e.stopPropagation(); closeWindow(win.id)}} className="w-3 h-3 rounded-full bg-[#FF5F57] border border-[#E0443E] cursor-pointer hover:brightness-90"></div>
                                    <div onClick={(e) => {e.stopPropagation(); minimizeWindow(win.id)}} className="w-3 h-3 rounded-full bg-[#FEBC2E] border border-[#D89E24] cursor-pointer hover:brightness-90"></div>
                                    <div onClick={(e) => {e.stopPropagation(); toggleMaximize(win.id)}} className="w-3 h-3 rounded-full bg-[#28C840] border border-[#1AAB29] cursor-pointer hover:brightness-90"></div>
                                </div>
                                <div className="flex-1 text-center text-xs font-bold text-gray-500/80 pointer-events-none">{win.title}</div>
                            </div>
                            <div className="flex-1 overflow-hidden relative bg-white">
                                {win.id === 'resume' ? <ResumeApp isAdmin={isAdmin} user={userData} updateUser={updateUser} /> : 
                                 win.id === 'portfolio' ? <PortfolioApp isAdmin={isAdmin} projects={projectData} updateProjects={updateProjects} /> : 
                                 win.id === 'calendar' ? <CalendarApp isAdmin={isAdmin} events={eventData} updateEvents={updateEvents} /> : 
                                 <MailApp user={userData} />}
                            </div>
                            {!win.maximized && <div className="resize-handle" onMouseDown={(e) => onResizeMouseDown(e, win.id)}></div>}
                        </div>
                    ))}

                    <div className="absolute bottom-4 left-1/2 transform -translate-x-1/2 glass px-3 pb-3 pt-3 rounded-2xl flex items-end gap-4 shadow-mac-dock z-[100] border border-white/20">
                        {APPS.map(app => (
                            <div key={app.id} onClick={() => openApp(app.id)} className="group relative flex flex-col items-center transition hover:-translate-y-2 cursor-pointer">
                                <GenericIcon app={app} />
                                {windows.find(w => w.id === app.id) && <div className="w-1 h-1 bg-black/50 rounded-full mt-1"></div>}
                            </div>
                        ))}
                    </div>
                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<MacOS />);
    </script>
</body>
</html>